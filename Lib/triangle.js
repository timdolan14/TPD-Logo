const Shape = require("./shape");

class square extends Shape {
    constructor() { }
    if(option === "triangle") {
    render() = {
        return: `<triangle>`
    }
}
};


module.exports = triangle;